[package]
name = "shadowplay-notify"
version = "0.1.0"
edition = "2021"

# Toast overlay. egui only; its Win32 calls are raw externs.
[dependencies]
eframe = { version = "0.29", default-features = false, features = ["glow", "default_fonts"] }
